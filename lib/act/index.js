define(['./Append', './Remove', './Replace', './Splice'], function (Append, Remove, Replace, Splice) {
    return {
        Append : Append,
        Remove : Remove,
        Replace : Replace,
        Splice : Splice
    };
});
